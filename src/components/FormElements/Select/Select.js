import React from "react";

function Select(props) {
  const { name, id, label, options } = props;
  return (
    <>
      <select name="" id="">
        <option value=""></option>
      </select>
    </>
  );
}

export default Select;
